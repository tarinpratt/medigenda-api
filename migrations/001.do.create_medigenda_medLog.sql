CREATE TABLE medlog (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    date DATE NOT NULL,
    time TIME NOT NULL,
    medname TEXT NOT NULL,
    amounttaken TEXT NOT NULL,
    reason TEXT NOT NULL
);

